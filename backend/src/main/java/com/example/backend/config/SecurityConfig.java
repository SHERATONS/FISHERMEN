package com.example.backend.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SecurityConfig {

    // This class is now empty. The PasswordEncoder bean has been removed.
    // You can add other application-wide beans here in the future if needed.
}
